#!/usr/bin/env python3

import os


executables = []
for dir_name in os.getenv("PATH").split(":"):
    if os.path.exists(dir_name):
        for name in os.listdir(dir_name):
            executables.append(name)

for name in sorted(executables):
    print(name)
